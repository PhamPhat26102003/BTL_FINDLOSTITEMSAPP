<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindLostItemsApp"
        tools:targetApi="31">
        <activity
            android:name=".pages.post.PostDetailActivity"
            android:exported="false" />
        <activity
            android:name=".pages.post.Post"
            android:exported="false" />
        <activity
            android:name=".pages.home.Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pages.register.Register"
            android:exported="false" />
        <activity
            android:name=".pages.login.Login"
            android:exported="false" />
        <activity
            android:name=".pages.home.MainActivity"
            android:exported="false" />
    </application>

</manifest>